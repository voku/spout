language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - phpenv config-rm xdebug.ini

install:
  - composer install --no-interaction --prefer-source

script:
  - php vendor/bin/phpunit --group=perf-tests --process-isolation
